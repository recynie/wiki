---
title: CSP202509A 蒙特卡洛
date: 2026-03-12
description: 第39次CCF-CSP（2025-09）A题：蒙特卡洛
tags:
  - simulation
draft: false
permalink:
---
```cpp
#include<bits/stdc++.h>
using namespace std;

int n,a;

int main(){
	ios::sync_with_stdio(false);cin.tie(nullptr);
	cin>>n>>a;
	vector<pair<double,double>> p(n);
	for(int i=0;i<n;i++)cin>>p[i].first>>p[i].second;
	int m=0;
	for(int i=0;i<n;i++){
		double x=p[i].first,y=p[i].second;
		if(x*x+y*y<=a*a){
			m++;
		}
	}
	cout<<fixed<<setprecision(6)<<(double)m/n*4;
}
```